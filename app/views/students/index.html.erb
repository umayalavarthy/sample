<center><h3>Listing Students</h3>

<table border="1">
  <tr>
    <th>Student Name</th>
    <th>Student ID</th>
    <th>Standard</th>
        <th>Course</th>
    <th>Rank</th>
    <th>Conduct</th>
    <th></th>
   <th></th>

  </tr>
 <tr>
<% @student.each do |student| %>
    <td><%= student.name %></td>
    <td><%= student.student_id %></td>
    <td><%= student.standard %></td>
    <td><%= student.course %></td>
    <td><%= student.rank %></td>
    <td><%= student.conduct %></td>


    <td><%= link_to 'Show', student %>
    </td>

    <td><%= link_to 'Delete',student, :confirm => 'Are you sure?', :method => :delete %></td>

  </tr>
<% end %>

</table><br/><br/>



<%= link_to 'New Student Form', :controller=>'students', :action=>'new' %><br/><br/>
<%= link_to 'Search Student', :controller=>'students', :action=>'search' %><br/><br/>
<%= link_to 'Student courses', :controller=>'students', :action=>'find1' %><br/><br/>
<%= link_to 'Back', :controller=>'admissions', :action=>'index' %><br/></center>